namespace Mission1SebastianMcFarland;

public class rollDice
{
    public static int rollDice(totalRolls)
    {
       int min = 1;
       int max = 6;
       int dice1 = random.Next(min, max);
       int dice2 = random.Next(min, max);
    }
}